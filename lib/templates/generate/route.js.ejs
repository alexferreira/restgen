module.exports.<%= objectName %>Routes = function(<%= objectName %>Controller, app, restMvc){

  // Example route implemtation. Uncomment for an example of how to implement a custom route.
  // app.get('/<%= objectName %>.:format?', function(req, res, next) {

  //   console.log('Overriden list route');

  //   <%= objectName %>Controller.index(function(err, results) {
  //     if (err)
  //       next(new Error('Internal Server Error: see logs for details: ' +  err), req, res);
  //     else {
  //       if (req.params.format){
  //         if (req.params.format.toLowerCase() == 'json') {
  //             res.send(results.map(function(instance) {
  //                 return instance.toObject();
  //             }));
  //         }else
  //           next(restMvc.RestError.Badreq.create('The \'' + req.params.format + '\' format is not supported at this time.'), req, res);
  //       }
  //       else {
  //         res.render(controller.name, { collection: results.map(function(instance) {
  //           return instance.toObject();
  //         })});
  //       }
  //     }
  //   });
  // })
};
