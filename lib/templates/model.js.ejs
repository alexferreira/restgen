module.exports.<%= modelName %> = function (mongoose) {
  // Standard Mongoose stuff here...
  var schema = mongoose.Schema;

  mongoose.model('<%= modelName %>', new schema({
    textValue: String
  }, { versionKey: false }));

  return mongoose.model('<%= modelName %>');
};
